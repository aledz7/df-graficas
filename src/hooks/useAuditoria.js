import { useState, useEffect } from 'react';

export const useAuditoria = () => {
  
  const registrarAcao = (acao, tipoEntidade, idEntidade, detalhesAnteriores = null, detalhesPosteriores = null, justificativa = null) => {
    
  };

  return { registrarAcao, registrosAuditoria: [], vendedorAtual: null }; 
};